spring:
  application:
    name: simpsons

  datasource:
    url: jdbc:mysql://localhost:3306/simpsonsaudit?useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
