apiVersion: apps/v1
kind: Deployment
metadata:
  name: front
  namespace: ejemplito
spec:
  replicas: 1
  selector:
    matchLabels:
      app: front
  template:
    metadata:
      labels:
        app: front
    spec:
      containers:
      - name: front
        image: contenedoreskubev2.azurecr.io/frontend:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80